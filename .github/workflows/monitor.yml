name: Schedule Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule check
        run: |
          echo "=============================="
          echo "GitHub Actions schedule test"
          echo "実行日時:"
          date
          echo "=============================="
